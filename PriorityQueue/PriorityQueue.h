#ifndef PriorityQueue_h
#define PriorityQueue_h

#include <stdio.h>
#include <stdlib.h>

struct PriorityQueue{
    int size;
    int * A;
    int length;
};

int insertPQ(struct PriorityQueue *p){
    if(p == NULL){
        printf("Invalid PriorityQueue.\n");
        return -1;
    }
    
    
    
    
    
    return 0;
}

#endif /* PriorityQueue_h */
